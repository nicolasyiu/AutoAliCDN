require "spec_helper"

describe AutoAliCDN do
  it "has a version number" do
    expect(AutoAliCDN::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
